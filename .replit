modules = ["nodejs-22", "postgresql-16"]
run = "npm start"

[deployment]
build = ["sh", "-c", "npm install && npm run build"]
run = ["sh", "-c", "npx tsx server/index.ts"]
deploymentTarget = "autoscale"

[[ports]]
localPort = 3000
externalPort = 80
